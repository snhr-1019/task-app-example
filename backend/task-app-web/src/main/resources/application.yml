spring:
  output:
    ansi:
      enabled: ALWAYS

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/taskapp?characterEncoding=utf8
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver

# TODO separate to application-development.yml
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
